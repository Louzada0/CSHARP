using System.Dynamic;

public class Calculadora
{
    public bool Soma (get; set;)}
    public bool Subtrair (get; set;)}
    public bool Multiplicar (get; set;)}
    public bool Dividir (get; set;)}
     
     public string DadosCalculadora()
        {
            return $"Título: {this.Titulo}"+
                   $"Autor: {this.Autor}";
        }

    }